# Проект "Отслеживание Сноубордиста"

## 🎯 Цель проекта

Цель данного проекта — разработать систему компьютерного зрения, имитирующую автономное слежение дрона за движущимся объектом (сноубордистом), удерживая его в центре кадра. Проект будет включать этапы детектирования объекта, отслеживания и, возможно, управления виртуальной "камерой" для поддержания цели в поле зрения.

## 📁 Структура Проекта

```
snowboard-tracking/
├── notebooks/
│   ├── 01_Data_Curation_and_Split.ipynb     # Скрипт для подготовки и разбиения датасета.
│   └── 02_Model_Training_and_Evaluation.ipynb # Скрипт для обучения, валидации и оценки модели.
├── resources/
│   ├── annotations/                      # Аннотации в формате YOLO для всех размеченных изображений.
│   ├── dataset/                          # [Генерируется] Финальная структура датасета, готовая к обучению (изображения и метки, разбитые на train/val/test).
│   │   └── dataset.yaml                  # Конфигурация датасета YOLO для Ultralytics.
├── runs/
│   └── detect/
│       ├── test_yolov8n_snowboarder_v1/  # [Генерируется] Результаты отдельного запуска тестирования модели (если задано имя).
│       └── yolov8n_snowboarder_v1/       # [Генерируется] Основные результаты обучения модели YOLOv8n (веса, графики обучения, логи).
├── scripts/                              # Вспомогательные Python скрипты для обработки данных и подготовки датасета.
│   ├── copy_test_data.py                 # Копирует аннотацию для test выборки из annotations в dataset.
│   ├── create_all_frames.py              # Получение всех кадров из видео.
│   ├── select_test_frames.py             # Нужен для получения списка неиспользованных кадров в train (удобно при ручном отборе кадров для test)
│   └── split_train_val.py                # Делит отобранные кадры для обучения на train и val
├── .gitignore                            # Файлы/директории, игнорируемые Git.
└── requirements.txt                      # Python зависимости проекта.
```

## 🚀 Текущий статус: Модель детектирования обучена и протестирована

На данный момент успешно завершены этапы подготовки данных, а также обучение и тщательная оценка модели обнаружения объектов. Модель демонстрирует выдающиеся результаты, что является прочной основой для реализации фазы отслеживания.

### **Фаза 1: Подготовка Данных**

**Цель:** Подготовить высококачественный датасет для обучения модели детектирования объектов, способной распознавать сноубордистов.

Полные детали, обоснование принятых решений и пошаговые инструкции по процессу курации и разделения данных подробно задокументированы в Jupyter Notebook: [01_Data_Curation_and_Split.ipynb](notebooks/01_Data_Curation_and_Split.ipynb).

**Краткий обзор выполненных шагов:**

1.  **Извлечение кадров из исходного видео**: Все доступные кадры из `resources/snowboard_day.mp4` были извлечены и сохранены в `resources/all_frames/` с использованием скрипта `scripts/create_all_frames.py`.
2.  **Ручная курация и отбор данных**:
    * **Обучающая и валидационная выборки**: 207 высококачественных кадров были вручную отобраны из `resources/all_frames/` и размещены в `resources/train_val_raw/`. Отбор проводился с целью обеспечения максимального разнообразия поз сноубордиста, условий освещения и ракурсов, а также включения сложных сценариев (изменение масштаба, разные уровни освещенности, позы и расположение сноубордиста, а также появление/отсутствие красного жилета).
    * **Независимая тестовая выборка**: Дополнительно, 63 уникальных кадра были вручную отобраны из *неиспользованных ранее* изображений в `resources/all_frames/` и помещены в `resources/test_raw/`. Скрипт `scripts/select_test_frames.py` использовался для идентификации неразмеченных ранее кадров. Эта независимая выборка критически важна для объективной оценки обобщающей способности финальной модели.
3.  **Ручная аннотация**: Все отобранные кадры (`train_val_raw` и `test_raw`) были вручную размечены в формате YOLO. Все файлы аннотаций (`.txt`) централизованно хранятся в `resources/annotations/`.
4.  **Формирование финального датасета**:
    * Изображения и аннотации из `resources/train_val_raw/` были разделены на тренировочную и валидационную части с помощью скрипта `scripts/split_train_val.py`.
    * Изображения и аннотации из `resources/test_raw/` были скопированы в соответствующую тестовую часть финального датасета с использованием скрипта `scripts/copy_test_data.py`.
    * Финальная, готовая к обучению структура датасета расположена в `resources/dataset/` и конфигурируется через `resources/dataset.yaml`.

**Статистика Датасета:**
* **Обучающая выборка (Train):** 166 изображений
* **Валидационная выборка (Validation):** 41 изображение
* **Тестовая выборка (Test):** 63 изображения
* **Общее количество аннотированных изображений:** 270
* **Классы для разметки:** Один класс — `snowboarder`.
* **Правила разметки:** Ограничивающие рамки рисуются вокруг **человека и его сноуборда**, максимально плотно охватывая оба элемента. *Важно отметить, что другие люди в кадре не размечались, что может повлиять на детекцию всех сноубордистов.*
* **Разрешение изображений:** 1920x1080 пикселей (Full HD).

### **Фаза 2: Обучение и Оценка Модели Обнаружения Объектов**

**Цель:** Обучить и оценить современную модель обнаружения объектов, способную точно идентифицировать сноубордистов.

Полные детали процесса обучения и оценки задокументированы в Jupyter Notebook: [02_Model_Training_and_Evaluation.ipynb](notebooks/02_Model_Training_and_Evaluation.ipynb).

#### Используемая Модель: YOLOv8n

* **Базовая Модель:** Предварительно обученная `yolov8n.pt` (nano-версия) от Ultralytics.
* **Параметры Обучения:**
    * Эпохи: 100
    * Размер изображения (`imgsz`): 640x640
    * Размер батча: Автоматически подобран (например, 26)
    * Оптимизатор: AdamW (по умолчанию для YOLOv8)
    * Скорость обучения: Автоматически настроена (по умолчанию для YOLOv8)

#### Результаты Обучения (на Валидационном Наборе)

* Precision (P): ~0.969
* Recall (R): ~1.000
* mAP50: ~0.992
* mAP50-95: ~0.868
* Скорость инференса: ~769 FPS на NVIDIA GeForce RTX 2060 (примерно 1.3 мс на изображение)

#### Оценка Модели на Независимом Тестовом Наборе

Модель была тщательно оценена на полностью невидимом ранее тестовом датасете (63 изображения, 63 экземпляра).

* **Метрики на Тестовом Наборе:**
    * Precision (P): **0.984**
    * Recall (R): **1.000**
    * mAP50: **0.995**
    * mAP50-95: **0.842**

Эти результаты демонстрируют исключительную способность модели обнаруживать сноубордистов с высокой точностью и полнотой даже на невиданных данных, что делает ее крайне подходящей для задач отслеживания в реальном времени.

## 🛠️ Используемые инструменты

* **Python 3.9.10** (в изолированной виртуальной среде `venv`)
* **Ultralytics YOLOv8**: Фреймворк для обучения и использования моделей YOLO.
* **OpenCV (`cv2`)**: Для работы с видеопотоком и извлечения кадров.
* **LabelImg**: Инструмент для ручной разметки изображений (создания ограничивающих рамок).
* **Jupyter Notebook**: Для интерактивной разработки и документации.

## ➡️ Как Запустить Проект

1.  **Клонировать Репозиторий:**
    ```bash
    git clone [https://github.com/your_username/snowboard-tracking.git](https://github.com/your_username/snowboard-tracking.git)
    cd snowboard-tracking
    ```
2.  **Создать Виртуальное Окружение (Рекомендуется):**
    ```bash
    python -m venv venv
    # В Windows:
    .\venv\Scripts\activate
    # В macOS/Linux:
    source venv/bin/activate
    ```
3.  **Установить Зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
    (Убедитесь, что `requirements.txt` содержит `ultralytics`, `ipython`, `matplotlib`, `pyyaml`, `numpy`, `opencv-python`)
4.  **Подготовить Исходные Данные:**
    * Поместите ваше исходное видео (`snowboard_day.mp4`) в `resources/`.
5.  **Выполнить Подготовку Датасета:**
    * Запустите Jupyter Notebook `notebooks/01_Data_Curation_and_Split.ipynb`, чтобы извлечь кадры, подготовить структуру данных и разбить датасет.
    * **Важно:** Для ручной аннотации вам понадобится инструмент LabelImg. Размещенные `.txt` файлы аннотаций должны быть помещены в `resources/annotations/`.
6.  **Обучить и Оценить Модель:**
    * Запустите Jupyter Notebook `notebooks/02_Model_Training_and_Evaluation.ipynb` для обучения модели YOLOv8n и оценки ее производительности.

## 🛣️ Дальнейшие Планы (Roadmap)

* Интеграция отслеживания экспериментов с Weights & Biases (W&B) для систематической настройки гиперпараметров и сравнения моделей.
* Исследование других вариантов YOLOv8 (например, `yolov8s`) или других архитектур детекции для потенциального улучшения производительности или оптимизации скорости.
* Реализация различных алгоритмов отслеживания (например, DeepSORT, ByteTrack) и сравнение их производительности в контексте задачи.
* Разработка пользовательского интерфейса для визуализации отслеживания в реальном времени.
* Оптимизация производительности для развертывания на целевом устройстве.